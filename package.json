{
  "name": "command_center",
  "version": "1.0.0",
  "description": "A multiservice integration for showing dev ticket info in a Github PR",
  "main": "src/index.ts",
  "engines": {
    "node": "10.16.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "build -- -w ",
    "dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/index.js"
  },
  "author": "Zan Rosenthal",
  "license": "ISC",
  "dependencies": {
    "@types/node": "~10.17.0",
    "fastify": "^2.10.0",
    "nodemon": "^1.19.4",
    "npm-run-all": "^4.1.5",
    "ts-node": "^8.4.1",
    "tslint": "^5.20.0",
    "typescript": "^3.5.3"
  },
  "devDependencies": {
    "@now/node": "^1.0.2"
  }
}
